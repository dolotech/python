if

elif

else

while


赋值
=  

对比
>
<
<=
>=
!=
==

+=    自加

def

return

{}

[]     数组

空值：None

布尔型 bool：True,  False

字符串 str： "adf"  '你的名字'  "123"

for in   遍历字典
 
break   跳出循环

type()    获取类型

len()

print()

str()		转换成字符串类型

int()      转换成整型

input()		获取输入	

range()       

del   删除字典里面的一个键值对或者删除数组中一个元素

list.pop()    删除列表一个元素，参数是数组下标，并且返回删除掉的元素,不带参数时，删除数组最后一个元素

list.append()  向列表尾部添加一个元素

list[:]   截取数组的部分元素，返回一个数组


list[0:0] = []   向数组插入或者删除元素


list.insert()   向数组中插入一个元素，第一个参数是数组下标，第二个参数是将要插入的元素

list.remove()  删除一个元素，参数是数组下标

list.extend()     数组拼接，参数是数组



